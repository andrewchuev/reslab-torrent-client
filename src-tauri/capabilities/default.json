{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capabilities for the torrent client",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:tray:default",
    "notification:default",
    "shell:default",
    "dialog:default"
  ]
}
